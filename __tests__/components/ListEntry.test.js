import Enzyme from 'enzyme';
import ListEntry from '../../client/components/ListEntry.jsx';

describe('ListEntry Component', () => {
  it('should be defined', () => {
    expect(ListEntry).toBeDefined();
  });
});