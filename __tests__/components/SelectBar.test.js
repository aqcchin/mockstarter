import Enzyme from 'enzyme';
import SelectBar from '../../client/components/SelectBar.jsx';

describe('SelectBar Component', () => {
  it('should be defined', () => {
    expect(SelectBar).toBeDefined();
  });
});